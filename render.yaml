services:
  - type: web
    name: gestor-tareas-telegram
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    runtime: python-3.11.9
